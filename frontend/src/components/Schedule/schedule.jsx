import React from 'react';
import { useParams } from "react-router-dom";
import axios from 'axios';
import Calendar from './calendar';

const Schedule = () => {
    const { scheduleId } = useParams();
    const [data, setData] = React.useState(null);
    const [schedule, setSchedule] = React.useState(null);

    // This code was generated by AI and significantly modified
    const fetchScheduleData = async () => {
        try {
            const response = await axios.get(`http://localhost:8080/api/schedule?id=${scheduleId}`);
            setData(response.data);
            setSchedule(Calendar(response.data));
        } catch (error) {
            console.error("Error fetching schedule data:", error);
        }
    };

    React.useEffect(() => {
        fetchScheduleData();

    }, [scheduleId]);

    return (
                <div>
                   { data ? <h1>{data.name}</h1> : <p> Loading... </p>}  {/* This line was generated by AI */}
                   { schedule ? schedule : <p> Loading... </p>}
                </div>
            );
}

export default Schedule;